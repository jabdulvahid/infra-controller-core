# nico-sim — Resume Context (2026-08-06)

## STATUS: Multi-site fabric working — dc-sim + ytl both running on jaslinux ✓

Both sites verified healthy with verify-fabric.py:
- dc-sim: underlay 7.x.x.x, overlay 8.x.x.x — old deployment (dc-simulation/output/)
- ytl:    underlay 9.x.x.x, overlay 10.x.x.x — new deployment (sites/ytl/fabric/)

---

## Major work completed 2026-08-06

### 1. Renamed dc-simulation → nico-sim
- Folder: `~/claude-notes/nico-sim/`
- Top-level doc: `nico-sim.md`
- YAML templates: `nico-sim.yaml`, `nico-sim-mac.yaml`

### 2. New nico-sim.yaml schema
Key additions vs old dc-sim.yaml:
- `dc_name` replaces `lab_name` (VM naming: `{dc_name}-{sitename}-{role}-N`)
- `sitename: site1` (Nico site name, separate from dc_name)
- All fabric prefixes /24→/16 for 1000-rack scale
- New address plan: 7.128/16 switch_underlay, 7.129/16 switch_loopbacks,
  7.130/16 dpu_fabric, 7.131/16 dpu_loopbacks, 7.132.0/30 internet_uplink,
  7.132.0.4/30 registry_link, 7.132.1/24 control_plane_prefix
- `underlay_pool: 7.140.0.0/14` (/24 per rack, replaces one-/16-per-rack)
- `fabric.evpn_vnis` table with NVIDIA production VNI numbers (verified from Nautobot)
- `fabric.underlay_leafs` with export_vni + relay per leaf
- `fabric.service_vnis` for external fabric services (no leaf needed)
- `fabric.datacenter_asn: 4266050000` (= switch_asn_base - 1, verified from Nautobot)
- `control_plane.site_controller_routes_asn` (for startupSMN Layer 3)
- `control_plane.smn_route_policy` (startupSMN route-import/export)
- `nico_container_registry` replaces `utility` (explicit `fabric_link`, `enabled` flag)
- `mh_dpu.count: 1` standalone DPU VM for sim-dpu-agent.py testing
- `postgresql.synchronous_mode` field

### 3. dc-sim-mac.yaml — new standalone MacBook profile
Single CP node, no MH VMs, smaller sizing, local registry kept for dev.
postgresql.instances: 1, synchronous_mode: false.

### 4. create-new-site.py — updated for new schema
```bash
./create-new-site.py ytl ~/sites/ytl 9 10
```
- Two mandatory positional args: underlay_octet + overlay_octet
- Generates both {site}.yaml and {site}-mac.yaml
- OOB block formula: 200 + ((u-7)//2)*20 per pair
- Bridge names include site name (br-{site}-internet, etc.)

### 5. generate-fabric.py — full new schema support
Key changes:
- `dc_name` → ContainerLab lab name + all bridge names
- All bridge names site-specific: `br-{dc_name}-cp{i}`, `br-{dc_name}-internet` etc.
- `leaf-mat` named correctly (not leaf-6), connects to br-{dc_name}-internet bridge node
- EVPN route-target export/import per leaf type
- MAT underlay prefix advertised into BGP on leaf-mat
- deploy.sh: flush stale veth interfaces before clab deploy (idempotent retry)
- deploy.sh: `clab destroy --cleanup` before deploy
- fabric/ output directory (was output/)

### 6. destroy-fabric.py — new conservative teardown script
```bash
sudo ~/claude-notes/nico-sim/destroy-fabric.py ~/sites/ytl
```
6 steps: clab destroy → orphaned veths → bridges → routes → dnsmasq → Docker network.
Supports --dry-run. Does NOT touch VMs, Nico, or other sites.

### 7. verify-fabric.py — updated for new schema
- Dynamic internet bridge name (br-{dc_name}-internet)
- Auto-detects site yaml from parent of fabric/ directory
- Host-ping downgraded to warning (non-critical)

---

## Multi-site address plan

| Octet pair | Site | Underlay | Overlay |
|------------|------|----------|---------|
| 7 + 8 | dc-sim (dc1) | 7.x.x.x | 8.x.x.x |
| 9 + 10 | ytl | 9.x.x.x | 10.x.x.x |
| 11 + 12 | (future) | 11.x.x.x | 12.x.x.x |

OOB blocks: 200 + ((u-7)//2)*20 → dc-sim=200-219, ytl=220-239

---

## ytl fabric verified healthy
```bash
~/claude-notes/nico-sim/verify-fabric.py ~/sites/ytl/fabric/topo.clab.yml --switch-only
# FABRIC HEALTH: ✓ HEALTHY — all BGP Established, all loopbacks reachable
```

All 9 containers running (super-spine, spine-1/2, leaf-1..5, leaf-mat).
BGP: all switch-to-switch sessions Established with EVPN.
Loopbacks: 72/72 pings pass (sub-millisecond RTT).

---

## Pending / next steps
1. Nico deployment on ytl (generate-nodes.py, form-k8s-cluster.py, deploy-nico-system.py)
2. Migrate dc-sim to new schema (requires fabric redeploy)
3. sim-dpu-agent.py implementation (mh-dpu-1 standalone DPU VM)
4. Layer 1 route-imports in generate-fabric.py (per underlay_leafs VNI config)
5. generate-nodes.py / generate-sim-values.py updates for new schema
6. attach-service.sh utility for fabric service containers
