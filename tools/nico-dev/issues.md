# nico-dev Issue Registry

Convention (adopted 2026-08-23): every issue either of us finds gets an ID
**`YYYYMMDD-#N`** — the date it was found plus a per-day sequence number.
The ID is assigned in conversation the moment the issue is found, and one
line lands here so the ID stays resolvable forever. Refer to issues by ID
("fix 20260823-#1") instead of re-describing them.

Status: OPEN → FIXED (commit) | WONTFIX (reason) | TRACKED-ELSEWHERE (link).

| ID | Status | Issue | Fixed in / notes |
|---|---|---|---|
| 20260823-#1 | FIXED | `ndev bgp info` run on the Mac silently reported "0 sessions" — the collector queried local docker (colima, no clab containers) instead of erroring. | bgp + fabric collectors return an error when zero clab containers exist; renderers print it as a warning. Verified live on the Mac. |
| 20260823-#2 | OPEN (upstream MAT bug) | Single Ctrl-C with `tui_enabled=false` exits MAT instantly WITHOUT running cleanup_on_quit (no force_delete_machine calls; machines stay READY in nico). The AppEvent::Quit path blocks on cleanup, so the signal path bypasses it. Repro: run-mat.sh, Ctrl-C once, machine table unchanged. | INTERMITTENT: second repro attempt (2026-08-23) cleaned up correctly on a single Ctrl-C. First occurrence may have been a double-press or timing-dependent. Needs deliberate repro before filing upstream; workaround: force-delete via admin-cli |
| 20260823-#3 | FIXED | MAT reruns don't reset the cluster to t0: cleanup_on_quit deletes machines only, leaving site-explorer endpoint history (incl. the lockout guard), expected-machine registrations, and DHCP lease state. Manifestation: NICO-SITEEXPLORER-144 'will not explore to avoid lockout' blocked the next run's endpoints after a stop/force-delete/restart cycle; cleared with site-explorer clear-error + refresh per IP. Site-wide credentials are day-1 config and should survive — the gap is MAT-fleet state. | ROOT CAUSE CONFIRMED live: nico ROTATES BMC creds during ingestion and stores them per-MAC in Vault (secrets/machines/bmc/<mac>/root). Deterministic MACs mean the next fleet reuses the same MACs; explorer prefers the stored rotated cred → fresh factory-cred mock rejects → lockout. Endpoint deletion alone does NOT fix it. Remediation recipe: (1) vault kv metadata delete secrets/machines/bmc/<mac>/root for each fleet MAC, (2) site-explorer clear-error + refresh per IP. FIXED: reset-mat-state.py — default scope wipes fleet state (machines, expected machines, endpoints, Vault per-MAC creds; day-1 config survives); --full recycles site defaults in one go: wipes them AND re-creates all three via mandatory --password (user's full-t0 ruling). Run after stopping MAT, before the next run. |
| 20260824-#1 | OPEN (upstream DX gap) | No CLI/API way to check whether site-wide default credentials are SET (BMC root, UEFI dpu/host): admin-cli credential has only add/delete/rotate/force, nicocli only create. Existence/status (not values) is a real operator need — 'did I run day-1 creds?' currently requires reading Vault directly. | Workaround: vault kv get -mount=secrets machines/bmc/site/root (and all_{hosts,dpus}/site_default/uefi-metadata-items/auth) via vault-0 exec. Candidates: upstream `credential status` subcommand (set/unset + updated-at, no values); nico-dev could add an `ndev creds` view meanwhile. |
| 20260824-#2 | DOCUMENTED (upstream design limit) | Redeploying an OLDER tag after the repo checkout moved fails: the newer build's migrate job advances the DB migration ledger, and nico refuses to run an older binary against it ('migration X was previously applied but is missing') — no schema downgrade support. Hit during B5 revert (golden checkout pulled to new main between the baseline and test builds). | Rule: within one dev cycle, all tags must come from the same checkout lineage — revert by rebuilding a baseline tag from the CURRENT checkout, never by redeploying a pre-pull tag. Recovery: delete the crashlooping migrate job, build+redeploy a fresh tag from the current checkout. Polish candidate: build script records checkout SHA per tag in {site}/tags.log. |
| 20260825-#1 | DOCUMENTED (cosmetic) | After reset-mat-state.py, endpoint rows RESURRECT in the admin GUI/site-explorer with stale SITEEXPLORER-101 errors while MAT is down: the script deletes the endpoint records, but nico re-creates them from persisted DHCP lease records and keeps probing (refused — nothing listens). Looks like a failed reset; is not. | The rows are blank slates (Initial state, no MachineId); the last-error text is overwritten by the first successful probe once MAT is up. Discriminator: an endpoint still showing 101 two minutes after MAT's aliases+listeners are up is a real failure; before that it's stale text. RESOLVED (2026-08-25, user ruling 'a clean reset should clean dhcp cache too'): the resurrection seed is the machine_id=NULL `machine_interfaces` rows created by BMC DHCP (site-explorer enumerates BMC-type interfaces, not leases; `dhcp_entries` is a stub). Upstream DOES have an API: `machine-interfaces delete --mac-address` — reset-mat-state.py step 4 now deletes all fleet (02:/06:) interfaces, so endpoints stay gone after a reset. Related VM-side gap fixed same day: stale BMC aliases from a killed MAT survive resets (reset runs on the Mac) — run-mat.sh now clears mat_underlay aliases at every launch (claude-notes beef8b5, linux 275e486). |
| 20260825-#2 | FIXED | New-architecture MAT (upstream removed per-machine IP-alias creation + per-IP listeners, ~Aug 2026, between the Aug-21 stock build and the #5306 merge base) serves all mock BMCs from ONE 0.0.0.0:443 listener, Host-header-routed (machines register under their DHCP IP). The VM then accepts packets for the BMC prefix only via a kernel LOCAL ROUTE — which is invisible in `ip addr` and does not survive reboot. B6's reboot silently removed it; every Issue-4 test run failed with SITEEXPLORER-101 refused on all endpoints while MAT looked healthy. Proof: stock binary contains the string 'failed to add BMC mock address to interface'; the new binary does not. | run-mat.sh (both editions) now ensures `ip route add local <mat_underlay> dev br-<dc>-internet` on every launch, alongside the stale-alias cleanup (which still matters for legacy binaries). Doc: mat-in-nico-dev §6 bullet + failure-catalog row. Immediate fix needs no MAT restart — the registry is already populated. |
| 20260825-#3 | FIXED | The 20260825-#2 fix was too broad: a whole-/24 LOCAL route (`ip route add local <u>.140.2.0/24`) also claims the fabric gateway .1, so the VM swallows its own traffic toward the gateway. Broke discovery-phase machine DHCP (DhcpFailed, 11 retries) and chained into `discover_machine` PermissionDenied — hosts stalled before Ready while BMC ingestion looked fine. Diagnostic: `ip route get <u>.140.2.1` says `local`. The old per-machine /32 aliases never covered .1, which is why the legacy binary setup didn't have this failure mode. | run-mat.sh (both editions) replaces the /24 with a 7-CIDR local-route set covering .2-.255 and excluding .1 (.2/31 .4/30 .8/29 .16/28 .32/27 .64/26 .128/25), and deletes the old /24 if present. Recovery needs no MAT restart — the DHCP retry FSM picks up on its next attempt. |
| 20260825-#4 | FIXED | `redeploy-dev-nico.py` (helm upgrade) re-renders nico-api-config-files, silently DROPPING the `allow_insecure_discovery = true` patch that deploy-dev-nico.py applies post-deploy. B5's recovery redeploy (2026-08-24) removed it; nobody noticed because the OLD MAT sourced discovery from genuinely-owned machine IPs (the legacy assign-IPs-to-interface mode) and passed the check legitimately. New-architecture MAT (upstream #5227 removed that mode) cannot, so machine discovery fails PermissionDenied 'discovery source IP and selected interface do not identify one host' + DPU DHCP relay timeouts — reproduced on a CLEAN t0 run, exonerating the fleet-state and route theories. | Re-applied live (configmap patch + nico-api rollout restart, no MAT restart needed). Structural: redeploy-dev-nico.py (both editions) now re-applies patch_allow_insecure_discovery after every helm upgrade. Upstream context: flag documented 'integration tests only', section `machines`, default false. |
| 20260826-#1 | FIXED (nico-dev config; upstream race still open) | Defaults regression run (new MAT binary, stock config, realistic timings) wedged: DPUs stuck in Init at DHCP attempt 300+, `discover_dhcp` Internal error 'Got DHCP for predicted interface ... not of the expected type' (earlier tor, now underlay). MECHANISM: nico's site-explorer machine_creator, on creating a DPU machine from BMC pairing, predicts the DPU's oob_net0 → Underlay IF no interface row exists yet — arming a strict segment-type check at DHCP time; if the lease exists first it just associates it, NO check. Realistic timings make pairing beat the DPU's slow boot → prediction armed → MAT relays the DPU oob DHCP via 12.135.0.1 (legacy `admin_dhcp_relay_address`, which the renamed config schema reads as the UNDERLAY relay) → maps to the admin-typed segment → hard mismatch, retried forever. Accelerated runs pass because the lease wins the race (associate path). GUI symptom: DPU without an OOB IP on the managed host. | FIXED 2026-08-26 (claude-notes 57e3539): generators + site tomls now use canonical relay keys with underlay_dhcp_relay_address = <u>.140.2.1 — the legacy admin_ key name was upstream's admitted misnomer (#5229) for the DPU-OOB relay, which is how the admin-gateway value crept in. VERIFIED END-TO-END 2026-08-26: post-fix defaults run reached Ready zero-touch — all four DPU OOB predictions promoted ('moving to machine'), zero type errors, dpuinit→hostinit→ready at realistic timings. Remaining upstream items to file: (a) nico-dev config: point the underlay relay at 11.140.2.1 (rack-mat-hosts, underlay-typed) / adopt canonical relay key names post-rename; (b) upstream: file the associate-vs-predict order sensitivity — real hardware with a slow-booting DPU can hit the same permanent wedge. Feeds the Issue-4 default-behavior decision: the wedge is env-config + upstream race, NOT the Issue-4 window itself. |
| 20260826-#2 | FIXED | first-boot.sh died silently at Step 11 on the HAPPY path: with all pods already Running (the golden clone boots into a working cluster by design), `grep -v | wc -l` has grep exit 1 → pipefail fails the assignment → set -e kills the script — no poll output, no summary banner, no POST-SETUP.txt. Found live during C3a (vm1 import test); cluster itself was healthy. Same set-e/grep class as the bake vault-gate and run-mat log_file bugs. | grep -cvE with `|| true` on both count assignments. Fixed copy pushed into vm1's share for an immediate re-run; baked copy fixed at the v2 rebake (with host-key regen + share-removal rule + compressed export). |
| 20260826-#3 | FIXED | Two compounding first-boot defects found in C3a: (a) first-boot used a FLAT sites/<sitename> layout while every other nico-dev tool uses sites/<dc>/<site> — fabric regeneration scattered artifacts across BOTH trees (stray sites/sjc/fabric beside the real sites/ytl/); (b) bake leaves the maintainer's KUBECONFIG line in ~nico/.bashrc and first-boot appended only-if-absent, so every clone kept the maintainer's dead path. User-visible: wrong KUBECONFIG in .bashrc, duplicate sites dirs. | first-boot now uses sites/<dc>/<site> everywhere (incl. Mac-side summary paths) and REPLACES the .bashrc KUBECONFIG line. Fixed copy staged into vm1's share; clean rerun: rm -rf <share>/sites first. Baked copy fixed at the v2 rebake. |
| 20260826-#4 | FIXED | first-boot.sh SELF-OVERWRITE: when run from the share (the natural path for iterating on fixes), its own step 8 `cp -r /usr/local/lib/nico-dev/. <share>/nico-dev/` truncate-overwrites the RUNNING script in place (same inode); bash reads scripts incrementally by offset, so execution resumed mid-line inside different (older, baked) content: '────────: command not found'. Runs from /usr/local/lib were immune, which hid it through C3a runs 1-2. | Double fix: (a) entire body wrapped in main(){...}; main "$@" — bash parses the whole file before executing anything, immune to any mid-run overwrite; (b) step 8 uses rsync -a (temp+rename, fresh inodes) instead of cp -r (in-place truncate). Re-staged into vm1's share (the crash had clobbered the previous staged fix with the baked copy). |
| 20260826-#5 | FIXED (one polish item open) | ndev run from the Mac tracebacked: the fabric collector shells the Linux-only `ip` command unguarded (FileNotFoundError on macOS) — same off-host class as 20260823-#1, different call site. Found running ndev against the vm1 clone site. | _get_bridges wraps the `ip` call, returns [] off-host; collectors scanned — `ip` was the only Linux-only binary. Verified: ndev info renders from the Mac (k8s data live, fabric/BGP correctly n/a). Polish CLOSED 2026-08-31 (hit twice, fixed on the walkthrough run): the DPU collector now returns n/a off-host — 'DPU stand-in  n/a (VM-side — run ndev on the VM)'. |
| 20260826-#6 | DOCUMENTED (by-design trap) | Rebooting the BUILDER VM after baking boots it fabric-less: the bake clears /etc/nico-dev/env (correct for the image — first-boot writes it on clones), but nico-dev-fabric.service is EnvironmentFile-driven, so its ExecStart runs with an empty NICO_DEV_SITE and deploys nothing. Symptom: clab containers running (docker restart policy) but ALL BGP peers Active/up=never, all loopback pings timeout, br-<dc>-internet missing — looks like 'metallb broken' from the outside (VIP rides the fabric BGP). Clones are immune (first-boot restores env + restarts the service). | Remedy on the builder: restore env (NICO_DEV_SITE=<site yaml>) + systemctl restart nico-dev-fabric — fabric self-heals. Bake banner now warns about the post-bake boot state. |
| 20260826-#7 | DOCUMENTED (clone cold-start; remedy known) | On a golden-clone cold boot (vm2), patroni's leader-election window left nico-api's WorkLockManager pool rejecting read-only DB connections — and the pod sat 1/1 Running WITHOUT serving (probe gap), so the api VIP refused connections (no ready endpoints). Presents as 'fabric/metallb broken' and cost an evening of fabric archaeology; patroni itself showed Leader/healthy throughout. vm1 dodged it by boot-timing luck. | Remedy: once `patronictl list` shows Leader, `kubectl rollout restart deployment/nico-api -n nico-system` (fresh pool) — VIP answers ~30s later. Diagnosis order for 'VIP refused': FIRST `kubectl get endpoints -n nico-system` (no endpoints = api not serving, NOT fabric), then api logs for 'WorkLockManager requires a writable database connection'. Candidates: first-boot post-check (wait for patroni Leader then bounce api if rejects seen); upstream: api readiness probe should fail while the work-lock pool can't write. One-pager expectation line: first boot may need a few minutes + this restart. |
| 20260827-#1 | FIXED (preventions pending) | Mac disk exhaustion (98%, 432MiB free) mid NGC smoke test → vm2's qcow2 could not grow → guest I/O errors → KERNEL PANIC, killing the helm upgrade mid-flight. Root cause of the fill: colima's docker build cache (98.8GB!) + dangling images (12GB) — the bake prunes the VM's caches but nothing ever pruned the Mac side — plus the day's image sprawl (2 masters + 2 clone VMs + 2 original VMs ≈ 130GB of qcow2). macOS gave one quiet notification; the failure surfaced as a GUEST kernel panic, maximally misleading. | Reclaimed 111GB (`docker builder prune -af` + `docker image prune -f`). Preventions to implement: (a) deploy-nico-from-ngc.py pre-flight free-space check (a 10GB NGC image needs ~25GB across colima+registry+VM growth); (b) periodic Mac-side prune guidance in how-to (builder cache grows ~100GB/week of nico builds); (c) image-sprawl hygiene: delete superseded masters/clones promptly (APFS clones share blocks, mutated clones diverge to full size). Recovery: free space FIRST, then boot the panicked VM — journaled fs recovers; expect the B6 return + possibly the 20260826-#7 api dance. |
| 20260831-#4 | FIXED | dev-up used the FLAT site layout sites/<site> — the pre-2026-08-26 convention inherited from how-to §3's example — while first-boot and bake-golden-image use the NESTED sites/<dc>/<site> ruled in the 20260826 two-dirs fix. Same bug class, new entry point; found by Jasmeer inspecting the maiden-run VM (~/mac/sites/dev1/dev1.yaml). | dev-up site_vm/site_mac + final KUBECONFIG line now sites/<dc>/<site>; how-to §3-§9 examples and README updated to match. The maiden-run env (vm1) stays at its flat path — site yaml bakes absolute paths, so moving the dir would break it; point ndev at the flat path there or rebuild. |
| 20260901-#7 | DOCUMENTED (UX hazard; advice fixed) | The Built-in Terminal serial console we advise adding is a trap: UTM ties the VM's LIFECYCLE to its window — closing the console window STOPS the VM (user-tested twice; a 'rescue console' that kills the cluster on Cmd-W). Also still blank on pre-getty VMs (vm1's seed predates the serial-getty runcmd; the combined getty+Built-in-Terminal case is still unobserved). | Manual-step advice now leads with the warning (hide/minimize, never close) and promotes the safe path: a Pseudo-TTY serial read externally via screen — quitting screen never touches the VM. Follow-up candidates: check whether UTM offers a keep-running-on-window-close preference; observe a post-getty VM with Built-in Terminal. |
| 20260901-#6 | FIXED | nico-dhcp crashlooped on the fresh NGC run: kea hook path rendered as /usr/lib/arm64-linux-gnu/ (no such dir — the gnu triplet is aarch64). The arch pass substituted platform.machine() assuming generate_dev_values runs on the VM (Linux → 'aarch64'), but in the NGC lane deploy-dev-nico (and thus values rendering) runs ON THE MAC, where macOS says 'arm64'. Second arch-pass regression; run-location assumptions strike again (same family as 20260901-#5). | Triplet normalized: {'arm64':'aarch64'}.get(machine(), machine()) — correct whichever side renders, and on Intel (x86_64 both sides). Verified for all three machine strings. Heal on a live cluster: re-graft + dev-up --from ngc (helm upgrade re-renders values; dhcp pod recovers). |
| 20260901-#5 | FIXED | The 20260901-#4 early-IP win created an apt race: ssh (and dev-up's prep step) now arrive while first-boot cloud-init is STILL installing packages — prep's apt-get died on the apt lists lock held by cloud-init's apt. Before #4, ssh only came up after cloud-init finished, hiding the ordering assumption. Bonus find in the same run: prep's sizing warning (16GB/8cpu) predated the measured tiers and scolded the now-recommended 8GB run config. | prepare-vm.sh: (a) waits via `cloud-init status --wait` when first boot is still running, before any apt use; (b) sizing thresholds aligned to the measured tiers — warn below 8GB RAM / 4 CPUs, note 12GB/6 as comfortable. |
| 20260901-#4 | FIXED (seed network-config) | First boot after the arch pass: VM booted fine but came up DHCP-only (.26), no static .126 — looked like boot death because serial/display are normally silent on this image. Root cause: the arch pass renamed the netplan stanza enp0s1 → nic0 (driver-match); cloud-init's fallback DHCP stanza is named enp0s1, and networkd applies only the FIRST matching .network file in sort order (enp0s1 < nic0) → DHCP won. The old config had only worked by ID-COLLIDING with the fallback and merging over it. | Static net config moved to the NoCloud seed's network-config file — replaces the fallback entirely (no conflict possible) AND applies at early boot: .126 is up in the first minute, before package installs, ending the slow-network ssh-wait ambiguity. driver-match retained (proven correct: readlink → virtio_net). Also EXONERATES 20260901-#3: that VM likely booted fine too, IP-less for the same reason. |
| 20260901-#3 | EXONERATED by #4 (device-add stays opt-in as caution) | VM created via the fixed flow (proven record + post-create update-configuration adding display/serial) STARTED but never booted: 'Display output is not active', serial pty silent, no IP, ssh-wait hang. Suspicion: the update-configuration round-trip rewrites the whole config and may mangle the boot setup (drives/UEFI) — the fetched drive records were already known-lossy (no guest size, 20260828-#3). Drives-panel inspection pending. | build-nico-dev-vm: post-create device-add is now OPT-IN (--serial-console, default OFF, marked experimental); default creation adds nothing after the proven make record. Console guidance = the manual Built-in Terminal serial (GUI). Recovery: delete VM, rerun --from vm. |
| 20260901-#2 | FIXED (docs+hints) | `utmctl attach` is NOT IMPLEMENTED in current UTM (prints 'WARNING: attach command is not implemented yet!' plus the PTY path) — every escape-hatch hint in our scripts/docs pointed users at a dead command. Found live when watching cloud-init on the serial console. Silver lining: the PTY path it prints proves the scripted post-create serial-device add (20260901-#1 follow-up) WORKS. | All hints now say: utmctl attach <vm> prints the PTY path, then `screen <pty> 115200` (Ctrl-A d to detach). |
| 20260901-#1 | FIXED (non-fatal follow-up) | The 20260831-#3 display+serial additions to the make-time record were never live-tested (every later run skipped creation) — first real create with them died -1700, same class as the directory-shares find: keys UTM rejects at make-time kill the whole record coercion. Tell in the echo: our `serial ports` term resolved as `serial port` (a different property). | Creation record reverted to the proven shape; display (virtio-gpu) + serial (ptty) now attempted POST-create via update configuration, check=False — on failure it prints the GUI add-a-Display steps and the run continues. Principle: never add untested keys to the make-time record; new device config goes through update configuration, non-fatally. |
| 20260831-#1 | FIXED | dev-up maiden run: step `site` died exit 255 with no useful output — bare step-ssh offered ALL agent keys (user had 8), sshd disconnected at MaxAuthTries before the authorized key's turn. The exact failure mode prepare-vm.sh's own comments document; prep VERIFIES with IdentitiesOnly, so it passed while the very next ssh failed. | vm_ssh pins `-i <configured key> -o IdentitiesOnly=yes` whenever ssh_key is set. Related hardening from the same run: fresh-VM runs scrub guaranteed-stale known_hosts entries for the VM IP + its /etc/hosts names (user request — "the errors people waste too much time on"); preflight (also under --dry-run) validates ssh_key file, share dir, UTM, NGC token env/image, docker-in-ngc-mode BEFORE any step; the printed resume command carries --config (it used to drop the user's yaml — wrong site, wrong mode); prepare-vm.sh gained --ssh-key so the config's key skips its interactive picker. |
| 20260831-#2 | FIXED (NGC-pull, fallback build) | THE find of the maiden run: NGC mode on a truly fresh site left nico-rest-db-migration in ImagePullBackOff — the six nico-rest-* images are separate Go-built images and deploy-nico-from-ngc.py only ever shipped the core image. The 2026-08-27 validation never saw it: that was a REDEPLOY over a golden clone with the REST stack already placed; `--initial` deploys REST and exposed the gap. Resolution path: setup.sh proves production serves core AND nico-rest-* from ONE registry base; live tags list confirmed CI publishes REST images per-PR with identical tags. | deploy-nico-from-ngc.py now pulls all six REST images from <ngc-base>/*:<tag>, retags into the local registry — zero-build AND version-matched with the core by construction. Fallback when a tag is missing: build-dev-nico-mac.py --rest-only (new flag: skips the Rust core, buildx-pushes the Go REST images in minutes) with a version-skew warning. Live heal verified: rerun --from ngc → migration Completed → full REST stack Running. |
| 20260831-#3 | FIXED (new VMs; GUI add for existing) | Script-created VMs had NO display device — the AppleScript creation record only gets what's asked, unlike the GUI wizard — so double-clicking the VM in UTM opened nothing; and no serial port either, so the boot-failure hint `utmctl attach` had nothing to attach to. | Creation record now carries displays:{{hardware:"virtio-gpu-pci"}} (UTM falls back to the default card if the name is unknown) + serial ports:{{interface:ptty}}. Existing VMs: UTM → Settings → New Device → Display, while stopped. Cosmetic cousin from the same run: the share-Path pause prompt should say "click into this terminal and press Enter" — after the GUI step, keyboard focus is guaranteed elsewhere. |
| 20260828-#4 | FIXED (self-healing rerun) | UTM `make new virtual machine` IMPORTS (copies) the source images into the VM bundle (`~/Library/Containers/com.utmapp.UTM/Data/Documents/<name>.utm/Data/`, seed ISO even converted to qcow2) — it does not reference the staging files. So the 20260828-#3 header-grow, applied to the staging disk AFTER the VM had been created (iteration-order artifact of the maiden run), never reached the guest: booted with vda 3.5G, root hit 100% during cloud-init package installs → `cloud-init status: error`. Fresh runs are unaffected (grow precedes create). | stage_vm's "VM already exists" branch now also grows the BUNDLE qcow2 (only when `utmctl status` = stopped — never patch a running VM's disk); after create it prints that ~/UTM-disks/<name> is only a rerun cache. Recovery on the live VM: poweroff → rerun script (bundle grows) → boot → `sudo cloud-init clean --logs && sudo reboot` (growpart/resizefs are per-boot; clean reruns the failed package phase; host keys regenerate → known_hosts warning). |
| 20260828-#3 | FIXED (header-grow) | UTM scripting cannot resize an existing QEMU drive, in three independently confirmed ways: `guest size` in the make-time drive record is silently ignored (drive stayed at cloud-image size); fetched drive records omit `guest size` entirely (only read-only `host size`); setting it via a config round-trip errors -10006. sdef's writable `guest size` is only honored when CREATING a new empty drive (no id/source). | build-nico-dev-vm.py now grows the qcow2 itself at stage image: pure-Python header patch (size @24, l1_size @36) — valid for a grow when no backing file/encryption/snapshots and the larger L1 entry count fits the already-allocated L1 cluster (all preconditions checked, zero-tail verified before writing). Tested on the real cloud image: 3G/7 L1 → 120G/240, idempotent. Removes the would-be `brew install qemu` prereq entirely. Guest-boot confirmation = lsblk 120G + growpart. |
| 20260828-#2 | FIXED (share path = 1 GUI step) | build-nico-dev-vm.py maiden run: UTM `make new virtual machine` failed -1700 ("Can't make {...} into type qemu configuration or apple configuration"). Cause: the record carried a `directory shares` list — that property exists ONLY on `apple configuration`; `qemu configuration` (UTM.sdef Configuration Suite) exposes just `directory share mode` (none/WebDAV/VirtFS) and NO share-path property at all — the share path of a QEMU VM is not scriptable. | stage_vm record fixed: `directory shares` removed, `directory share mode:VirtFS` added; the path is now a printed one-manual-GUI-step (Settings → Sharing → Path) with an interactive pause before boot in full runs. how-to §1A updated. Possible future: post-create config.plist edit while the VM is stopped (unverified — UTM may cache config). |
| 20260828-#1 | FIXED | First foreign-Mac certification run (colleague): UTM share not attached (Directory Share Mode was not VirtFS) → first-boot's step 4 WARNED but continued → steps 5-9 personalized the clone's LOCAL disk under /mnt/mac → fabric unit's mount dependency then failed ('A dependency job ... failed'). Extra finds: /mnt/mac/nico-dev's bake-era mtime is NOT proof of baked residue (rsync -a preserves source times — it may be the colleague's own step-8 copy; check under-mount residue at the v3 bake regardless), and the fstab already carries a root→nico bindfs layer (predates our backlog design), and the repos-folder prompt invited a repo-path answer (colleague gave the repo, not its parent). | first-boot now HARD-STOPS when the share is not a mountpoint (with VirtFS instructions); repos prompt reworded (PARENT folder); GETTING-STARTED step 1 names VirtFS explicitly. Remaining for v3 bake: clean local mountpoint residue (umount first) — noted, not yet in bake. Colleague remedy: set VirtFS + directory, reboot, rerun first-boot. |
