# nico-dev ↔ nico-dev-linux — Shared Known-Issues Ledger

Per the fork operating model (POR Phase G): the trees share NO code, but
every fix in either tree appends one line here so the other tree's fix is a
lookup, not a re-diagnosis. Security-class fixes port immediately; everything
else is demand-driven.

Format: date | found in | issue (one line) | fixed there in | other tree status

| Date | Found in | Issue | Fixed in | Other tree |
|---|---|---|---|---|
| 2026-08-24 | (fork point) | nico-dev-linux forked from nico-dev at claude-notes commit of this date — all fixes through 20260824-#1 included | — | in sync at fork |
